---
description: Nilou 项目硬约束（最小集合）
alwaysApply: true
---

# Nilou 硬约束（最小版）

## 沟通与一致性

- 若发现文档与代码不一致，需在终端输出警告并给出更新建议。
- 若用户设计指令存在歧义，必须先澄清再实施。

## 构建顺序（必须遵守）

1) 构建 `NilouHeaderTool`
```
xmake build NilouHeaderTool
```

2) 构建 `NilouShaderTool`
```
xmake build NilouShaderTool
```

3) 构建主目标（`NilouEditor` 或 `NilouGame`）
```
xmake build NilouEditor
```

## 模块可见性约束

- 运行时代码位于 `Engine/Source/Runtime`。
- 每个模块的 `Public` 目录会作为公共 include 路径对依赖者可见。

## 着色器硬约束

- 项目着色器语言为 Slang。
- 只有 `Engine/Shaders/Public` 下的着色器是全局可见。
- 每个着色器文件必须以 `module` 或 `implementing` 声明开头。
- 所有着色器参数必须封装在 `ParameterBlock<T>` 中。

## 反射与 Shader 生成规则来源

- HeaderTool 细则：`.cursor/skills/nilou-header-tool/reference.md`
- ShaderTool 细则：`.cursor/skills/nilou-shader-tool/reference.md`
- Graphics Pipeline 细则：`.cursor/skills/graphics-pipeline/reference.md`
